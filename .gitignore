node_modules/
dist/
coverage/
*.log
.env
.env.local
.DS_Store
reports/
*.tsbuildinfo
.vitest-cache/

# Keep dist-action committed — it's the bundled Action entry point.
# (dist/ is the library build output, .gitignored; dist-action/ is committed)
